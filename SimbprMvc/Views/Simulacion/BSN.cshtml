@model SimbprMvc.Models.ViewModels.SimulacionBSNViewModel
@{
    ViewData["Title"] = $"BSN — {Model.ProyectoNombre}";
}

<div class="d-flex align-items-center gap-2 mb-4">
    <a asp-controller="Simulacion" asp-action="Index" asp-route-proyectoId="@Model.ProyectoId"
       class="btn btn-sm btn-outline-secondary">← Simulador</a>
    <h5 class="mb-0 fw-bold">⚙️ Bomba Sumergible (BSN/ESP) — @Model.ProyectoNombre</h5>
</div>

<partial name="_BSNPanel" model="Model" />

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
